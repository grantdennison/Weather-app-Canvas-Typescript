/** @type {import('ts-jest').JestConfigWithTsJest} */
module.exports = {
  preset: "ts-jest",
  testEnvironment: "node"
};

// function change Degrees C to F and distance km/h to mph

// updateText();
